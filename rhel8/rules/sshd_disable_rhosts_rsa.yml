description: 'SSH can allow authentication through the obsolete rsh

  command through the use of the authenticating user''s SSH keys. This should be disabled.

  <br /><br />

  To ensure this behavior is disabled, add or correct the

  following line in <tt>/etc/ssh/sshd_config</tt>:

  <pre>RhostsRSAAuthentication no</pre>'
identifiers: {cce: 80900-4}
ocil: 'To check which SSH protocol version is allowed, check version of

  <tt>openssh-server</tt> with following command:

  <pre>$ rpm -qi openssh-server | grep Version</pre>

  Versions equal to or higher than 7.4 have deprecated the <tt>RhostsRSAAuthentication</tt>
  option.

  If version is lower than 7.4, run the following command to check configuration:

  To determine how the SSH daemon''s <tt>RhostsRSAAuthentication</tt> option is set,
  run the following command:

  <pre>$ sudo grep -i RhostsRSAAuthentication /etc/ssh/sshd_config</pre>

  If no line, a commented line, or a line indicating the value <tt>no</tt> is returned,
  then the required value is set.

  '
ocil_clause: the required value is not set
oval_external_content: null
platform: machine
prodtype: all
rationale: 'Configuring this setting for the SSH daemon provides additional

  assurance that remove login via SSH will require a password, even

  in the event of misconfiguration elsewhere.'
references: {cis-csc: '11,3,9', cobit5: 'BAI10.01,BAI10.02,BAI10.03,BAI10.05', cui: 3.1.12,
  disa: '366', hipaa: '164.308(a)(4)(i),164.308(b)(1),164.308(b)(3),164.310(b),164.312(e)(1),164.312(e)(2)(ii)',
  isa-62443-2009: '4.3.4.3.2,4.3.4.3.3', isa-62443-2013: SR 7.6, iso27001-2013: 'A.12.1.2,A.12.5.1,A.12.6.2,A.14.2.2,A.14.2.3,A.14.2.4',
  nist: 'CM-6(a),AC-17(a),AC-17(b)', nist-csf: PR.IP-1, ospp: FIA_AFL.1, srg: SRG-OS-000480-GPOS-00227}
severity: medium
template: null
title: Disable SSH Support for Rhosts RSA Authentication
warnings:
- {general: 'As of <tt>openssh-server</tt> version <tt>7.4</tt> and above,

    the <tt>RhostsRSAAuthentication</tt> option has been deprecated, and the line

    <pre>RhostsRSAAuthentication no</pre> in <tt>/etc/ssh/sshd_config</tt> is not

    necessary.'}
