description: 'By default, the SELinux boolean <tt>swift_can_network</tt> is disabled.

  If this setting is enabled, it should be disabled.


  To disable the <code>swift_can_network</code> SELinux boolean, run the following
  command:

  <pre>$ sudo setsebool -P swift_can_network off</pre>'
identifiers: {}
ocil: '

  Run the following command to determine if the <code>swift_can_network</code> SELinux
  boolean is disabled:

  <pre>$ getsebool swift_can_network</pre>

  If properly configured, the output should show the following:

  <code>swift_can_network --> off</code>'
ocil_clause: swift_can_network is not disabled
oval_external_content: null
platform: machine
prodtype: rhel7,rhel8
rationale: ''
references: {}
severity: medium
template:
  backends: {}
  name: sebool
  vars: {seboolid: swift_can_network}
title: Disable the swift_can_network SELinux Boolean
warnings: []
