description: 'To set the runtime status of the <code>kernel.dmesg_restrict</code>
  kernel parameter, run the following command: <pre>$ sudo sysctl -w kernel.dmesg_restrict=1</pre>

  If this is not the system default value, add the following line to a file in the
  directory <tt>/etc/sysctl.d</tt>: <pre>kernel.dmesg_restrict = 1</pre>'
identifiers: {cce: 80913-7}
ocil: 'The status of the <code>kernel.dmesg_restrict</code> kernel parameter can be
  queried

  by running the following command:

  <pre>$ sysctl kernel.dmesg_restrict</pre>

  The output of the command should indicate a value of <code>1</code>.

  If this value is not the default value, investigate how it could have been

  adjusted at runtime, and verify it is not set improperly. This has to be checked

  in all files in the <tt>/etc/sysctl.d</tt> directory and the deprecated

  <code>/etc/sysctl.conf</code>. You can verify this by running the following command:

  <pre>$ grep -r kernel.dmesg_restrict /etc/sysctl.conf /etc/sysctl.d</pre>

  '
ocil_clause: the correct value is not returned
oval_external_content: null
platform: machine
prodtype: rhel6,rhel7,rhel8,fedora,ol7,ol8,rhv4,ocp4
rationale: 'Unprivileged access to the kernel syslog can expose sensitive kernel

  address information.'
references: {anssi: NT28(R23), cui: 3.1.5, disa: '1314', hipaa: '164.308(a)(1)(ii)(D),164.308(a)(3),164.308(a)(4),164.310(b),164.310(c),164.312(a),164.312(e)',
  nist: SI-11, srg: SRG-OS-000132-GPOS-00067}
severity: medium
template:
  backends: {}
  name: sysctl
  vars: {datatype: int, sysctlval: '1', sysctlvar: kernel.dmesg_restrict}
title: Restrict Access to Kernel Message Buffer
warnings: []
