description: 'To prevent USB storage devices from being used, configure the kernel
  module loading system

  to prevent automatic loading of the USB storage driver.


  To configure the system to prevent the <code>usb-storage</code>

  kernel module from being loaded, add the following line to a file in the directory
  <code>/etc/modprobe.d</code>:

  <pre>install usb-storage /bin/true</pre>

  This will prevent the <tt>modprobe</tt> program from loading the <tt>usb-storage</tt>

  module, but will not prevent an administrator (or another program) from using the

  <tt>insmod</tt> program to load the module manually.'
identifiers: {cce: 80835-2}
ocil: '

  If the system is configured to prevent the loading of the <code>usb-storage</code>
  kernel module,

  it will contain lines inside any file in <code>/etc/modprobe.d</code> or the deprecated<code>/etc/modprobe.conf</code>.

  These lines instruct the module loading system to run another program (such as <code>/bin/true</code>)
  upon a module <code>install</code> event.

  Run the following command to search for such lines in all files in <code>/etc/modprobe.d</code>
  and the deprecated <code>/etc/modprobe.conf</code>:

  <pre>$ grep -r usb-storage /etc/modprobe.conf /etc/modprobe.d</pre>'
ocil_clause: no line is returned
oval_external_content: null
platform: machine
prodtype: wrlinux1019,rhel6,rhel7,rhel8,fedora,ol7,ol8,rhv4
rationale: 'USB storage devices such as thumb drives can be used to introduce

  malicious software.'
references: {cis-csc: '1,12,15,16,5', cobit5: 'APO13.01,DSS01.04,DSS05.03,DSS05.04,DSS05.05,DSS05.07,DSS05.10,DSS06.03,DSS06.10',
  cui: 3.1.21, disa: '366,778,1958', hipaa: '164.308(a)(3)(i),164.308(a)(3)(ii)(A),164.310(d)(1),164.310(d)(2),164.312(a)(1),164.312(a)(2)(iv),164.312(b)',
  isa-62443-2009: '4.3.3.2.2,4.3.3.5.1,4.3.3.5.2,4.3.3.6.1,4.3.3.6.2,4.3.3.6.3,4.3.3.6.4,4.3.3.6.5,4.3.3.6.6,4.3.3.6.7,4.3.3.6.8,4.3.3.6.9,4.3.3.7.2,4.3.3.7.4',
  isa-62443-2013: 'SR 1.1,SR 1.10,SR 1.13,SR 1.2,SR 1.3,SR 1.4,SR 1.5,SR 1.7,SR 1.8,SR
    1.9,SR 2.1,SR 2.6', iso27001-2013: 'A.11.2.6,A.13.1.1,A.13.2.1,A.18.1.4,A.6.2.1,A.6.2.2,A.7.1.1,A.9.2.1,A.9.2.2,A.9.2.3,A.9.2.4,A.9.2.6,A.9.3.1,A.9.4.2,A.9.4.3',
  nist: 'AC-19(a),AC-19(d),AC-19(e),IA-3,MP-7', nist-csf: 'PR.AC-1,PR.AC-3,PR.AC-6,PR.AC-7',
  srg: 'SRG-OS-000114-GPOS-00059,SRG-OS-000378-GPOS-0016,SRG-OS-000480-GPOS-00227'}
severity: medium
template:
  backends: {}
  name: kernel_module_disabled
  vars: {kernmodule: usb-storage}
title: Disable Modprobe Loading of USB Storage Driver
warnings: []
