# platform = multi_platform_all
# reboot = false
# strategy = disable
# complexity = low
# disruption = low
- name: Ensure telnet-server is removed
  package:
    name: "telnet-server"
    state: absent
