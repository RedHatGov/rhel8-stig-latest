# platform = multi_platform_all
# reboot = false
# strategy = enable
# complexity = low
# disruption = low
- name: Ensure tcp_wrappers is installed
  package:
    name: "tcp_wrappers"
    state: present
