# platform = multi_platform_all
# reboot = false
# strategy = enable
# complexity = low
# disruption = low
- name: Ensure openscap-scanner is installed
  package:
    name: "openscap-scanner"
    state: present
