# platform = multi_platform_all
# reboot = false
# strategy = enable
# complexity = low
# disruption = low
- name: Ensure subscription-manager is installed
  package:
    name: "subscription-manager"
    state: present
