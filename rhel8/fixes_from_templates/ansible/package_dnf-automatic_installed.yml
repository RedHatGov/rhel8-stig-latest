# platform = multi_platform_all
# reboot = false
# strategy = enable
# complexity = low
# disruption = low
- name: Ensure dnf-automatic is installed
  package:
    name: "dnf-automatic"
    state: present
