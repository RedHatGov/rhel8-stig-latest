# platform = multi_platform_all
# reboot = false
# strategy = disable
# complexity = low
# disruption = low
- name: Ensure abrt-plugin-logger is removed
  package:
    name: "abrt-plugin-logger"
    state: absent
