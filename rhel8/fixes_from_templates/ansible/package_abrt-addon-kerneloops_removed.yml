# platform = multi_platform_all
# reboot = false
# strategy = disable
# complexity = low
# disruption = low
- name: Ensure abrt-addon-kerneloops is removed
  package:
    name: "abrt-addon-kerneloops"
    state: absent
