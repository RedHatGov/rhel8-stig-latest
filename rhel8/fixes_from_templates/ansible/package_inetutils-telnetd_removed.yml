# platform = multi_platform_all
# reboot = false
# strategy = disable
# complexity = low
# disruption = low
- name: Ensure inetutils-telnetd is removed
  package:
    name: "inetutils-telnetd"
    state: absent
