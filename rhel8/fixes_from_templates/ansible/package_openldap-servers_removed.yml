# platform = multi_platform_all
# reboot = false
# strategy = disable
# complexity = low
# disruption = low
- name: Ensure openldap-servers is removed
  package:
    name: "openldap-servers"
    state: absent
