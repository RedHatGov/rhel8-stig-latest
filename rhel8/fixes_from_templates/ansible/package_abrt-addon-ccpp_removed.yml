# platform = multi_platform_all
# reboot = false
# strategy = disable
# complexity = low
# disruption = low
- name: Ensure abrt-addon-ccpp is removed
  package:
    name: "abrt-addon-ccpp"
    state: absent
