# platform = multi_platform_all
# reboot = false
# strategy = disable
# complexity = low
# disruption = low
- name: Ensure geolite2-city is removed
  package:
    name: "geolite2-city"
    state: absent
