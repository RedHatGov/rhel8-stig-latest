# platform = multi_platform_all
# reboot = false
# strategy = disable
# complexity = low
# disruption = low
- name: Ensure setroubleshoot is removed
  package:
    name: "setroubleshoot"
    state: absent
