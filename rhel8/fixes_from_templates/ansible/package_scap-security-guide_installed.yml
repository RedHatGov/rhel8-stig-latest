# platform = multi_platform_all
# reboot = false
# strategy = enable
# complexity = low
# disruption = low
- name: Ensure scap-security-guide is installed
  package:
    name: "scap-security-guide"
    state: present
