# platform = multi_platform_all
# reboot = false
# strategy = disable
# complexity = low
# disruption = low
- name: Ensure abrt-plugin-sosreport is removed
  package:
    name: "abrt-plugin-sosreport"
    state: absent
